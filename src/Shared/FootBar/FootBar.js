import React, { Component } from "react";
import { Layout } from "antd";

const { Footer } = Layout;

export default class FootBar extends Component {
  render() {
    return (
      <Footer style={{ textAlign: "center" }}>welcome hyunseo world!</Footer>
    );
  }
}
